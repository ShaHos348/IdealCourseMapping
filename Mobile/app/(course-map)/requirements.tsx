import React from "react";
import { View } from "react-native";
import RequirementsPane from "@/app/components/RequirementsPane";

export default function RequirementsTab() {
  return (
    <View style={{ flex: 1 }}>
      <RequirementsPane />
    </View>
  );
}
